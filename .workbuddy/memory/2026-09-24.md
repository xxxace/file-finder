# 2026-09-24

## file-finder 全量代码审查（Electron + Vue3 封面浏览器）

**产出**：`docs/CODE-REVIEW.md`（完整审查报告，含 24 项 bug 分级清单 + 4 个架构分支 + 改造顺序）

### 关键取证结论（后续改动都建立在这些数字上）
- 真实缓存库 `~/.file-finder/searchCache.db` = **108,104,606 字节 / 107 条记录**，平均 986 KB/条，最大 4.31 MB，单条中 **base64 占比 100%**。
- 另有备份库 `searchCache-20240630.db` 123 MB + 项目根目录遗留 `searchCache.db` 90 MB，**合计约 320 MB**。
- `electron/server/index.ts:133` 图片走 `getBase64(原图路径)`，不是缩略图；`:90` 视频缩略图的 `size: '320x240'` 被注释掉 → 缩略图是原始分辨率 PNG（一行注释 = 100 倍膨胀）。
- `readFolder`（`index.ts:105-142`）是串行 `for + await`，100 张图 = 100 次串行 IO + base64，全部阻塞后才 resolve。

### 重要发现：问题 4 的解药已在数据里
`fs.Stats.dev` 在 Windows 上**就是卷序列号**。已核对 libuv `src/win/fs.c`：
`fs__stat_assign_statbuf` 里 `statbuf->st_dev = stat_info.VolumeSerialNumber.LowPart;`
（序列号来自 `NtQueryVolumeInformationFile(FileFsVolumeInformation)`，与盘符无关。）
→ 现存每条缓存记录里的 `dev` 字段（如 `4270644338`）就是移动硬盘身份，可直接用于自动重映射，用户无需再手点「盘符变更」。

### 五个判断（用户 5 个问题）
1. 一次性返回全部 + base64 —— **不合理，是万恶之源**。把图片当数据传，丧失并发/缓存/渐进解码，还付 37% 膨胀。
2. 懒加载 —— **形式上写了（`index.vue:47,48` 有 `:lazy="true"`），实质是假懒加载**。base64 已在内存，lazy 只推迟解码。且 `v-for` 全量渲染无虚拟滚动。
3. BUG —— 已列 24 项，P0 五项：URL 未 encode、`item.files![0]` 崩溃、单个文件出错整目录返回空、`postAction` 不检查 code 导致「失败提示成功」、`app.listen` 无 error 处理。
4. 见上，卷序列号。
5. 「服务接口乱」—— 根因是**HTTP 这一层本就不该存在**（同机同进程组，该走 IPC）。次因：地址硬编码 5 处、返回值形状 4 种、错误只在 body 的 `code` 里。

### 自我纠错（初版报告 3 处推断错误，已被实测推翻）
1. **不是 4MB 原图**：432 张封面解码后中位 **800×538**，平均 base64 仅 **235 KB**。误把记录里 `size` 字段（那是整个文件夹总大小，含 8GB 视频）当成单张图大小。
2. **单条 payload 不是几十百 MB**：中位 **674 KB**，P90 2.48 MB，最大 4.31 MB（20 张封面）。回环 HTTP 下是百毫秒级。
3. **不是 500 倍**：去掉 base64 后 **103.1 MB → 1.5 MB = 69 倍**。
→ 真实痛点重排：不是「单次请求慢」，而是 **nedb 把 100MB 图片当文档常驻内存（实测启动 361ms / 堆峰值 314MB）+ append-only 从不 compact + 换盘符搬 100MB**。

### 新增实测证据
- `node_modules/nedb/lib/persistence.js:200` `persistNewState` 走 **`storage.appendFile`** → **append-only**，只有 `compactDatafile()`（:148）才重写。代码里从未调用 → 每次 update 追加完整文档（含 base64）。**换一次盘符 = 往 103MB 库里再追加 103MB。**
- 现有库 107 行 / 107 个唯一 `_id`（无重复）→ 印证从未发生过 update 追加。
- `D:/file-docker` 目录**存在但为空（0 文件）** → **视频封面功能从未生效**；且 cover 模式的 `handleCover` 对视频不生成 base64，即使生成了也没人读。
- 位图内存：432 张全解码 = 724 MB；单张中位 1.7 MB。
- 封面尺寸分布：宽 300–2560（中位 800），高 300–1600（中位 538）。

### 用户的三点质疑与我的修正（**重要，别再犯**）
1. **「为什么要砍 HTTP 走 IPC？前后端分离不是更简单？」** → 用户说得对，**我收回**。理由成立：性能瓶颈不在通信层（payload 降到 10KB 后 HTTP 开销无关紧要）；DevTools Network 可逐条看请求是真实价值。**教训：我推 IPC 是过度设计。** 改为保留 HTTP，只修安全面 / 端口崩溃 / URL 编码 + 收敛统一响应体。
2. **「我为了隐私不存明文图片文件」** → 必须尊重。同时要告知他一个事实：**base64 不是加密**，记事本能直接看到 `data:image/jpeg;base64,...`，所以现在的设计**没有技术上的隐私保护**，只提供「不容易被一眼看到」。→ 推荐方案改为**零新增文件**：不落盘、不进 DB，主进程内存 LRU 持有缩放后的 JPEG；独立 `GET /cover?path=...` 接口返回 JPEG 二进制。
3. **「缩略了会影响画质吗？」** → 不影响，反而现在才是浪费。实测 CSS：`--item-width: calc(100%/6)`，1200px 窗口下每格约 180×144 px。800px 封面缩到 480px 仍是显示区 2.7 倍（1920px 窗口 1.5 倍），超过 2 倍 HiDPI 经验值。缩放用 Electron 内置 `nativeImage`（`createFromPath().resize().toJPEG(82)`），**零新依赖**。

### 已执行的改动（P0 五连止血，用户选择「分层推进」）
`git diff`：4 文件 +57/-30，`npx vue-tsc --noEmit` 退出码 0、零输出。
- P0-1 URL 编码：`index.vue` fetchFolder / onRefresh 加 `encodeURIComponent(path)`；`HistoryTable` `toQueryStr` 对 value 编码。
- P0-2 崩溃：`index.vue:168` `item.files![0].name` → `item.files?.[0]?.name || \`${item.name}.${item.ext}\``（保留 cover 模式原有行为，只补 folder 模式的兜底）。
- P0-3 `index.ts` readFolder 循环体包 `try/catch`（原整个 for 一个 try，单项失败整个目录返回空）。
- P0-4 `src/utils/request.ts` 新增 `assertOk`，`postAction` / `deletAction` 检查 `json.code` 与 `response.ok` → 修「失败提示成功」。
- P0-5 `index.ts` `app.listen(...).on('error')` → 端口占用不再崩主进程。

### 第二次自我纠错（用户纠正我）—— file-docker 机制

**我错在**：拿 `D:/file-docker` 是空目录就断言「视频封面功能从未生效」。真相：**file-docker 是按「资源所在盘」创建的**，封面在 `H:/file-docker`，而 H 盘当时未挂载 —— **取样错误**。

**实测验证**（缓存里有 **2 条 `video` 条目**，`TST-612` / `TST-4519`）：
- 格式 **PNG、1280×720**，base64 **1.12–1.26 MB/张**
- 对比图片封面 jpeg 均值 **161 KB** → 视频封面是 **7.4 倍**
- 缓存 107 条**全部来自 `H:\`**（只有一块盘被扫过）
- 成因：`index.ts:90` 的 `size: '320x240'` 被注释掉 → 抽原始分辨率帧；且输出 PNG（无损，对大图极浪费）

**用户的 file-docker 设计意图（他自己说明的）**：封面属于哪个盘就在哪个盘建 `file-docker`，所以**他有意允许明文存储**，因为它等价于「跟盘走的缓存」。他自知「这样子一样会泄露」，不确定设计好不好，在向我求证。

**我的结论（已写进报告第四节）**：
- **他的方向是对的，我提议「改成不落盘」是错的**——那会丢掉「缓存跟盘走」这个 DB 放 C 盘做不到的核心收益。
- 第一性原理：**封面是派生物，原文件就明文躺在同一块盘同一目录**。看到封面的人本来就能看到原文件 → **同盘场景下不构成额外泄露**。真正有风险的只有：把封面聚合到某处/上传云端、或只把 file-docker 拷走。
- 真实缺口是「没做完整」而非「泄露」：①图片封面走 base64 进 C 盘 DB（**两套策略并存**，DB 因此 103MB）②视频封面尺寸失控 ③命名用剥扩展名的 `name` 会互相覆盖（P1-8）。
- **正确方向：把图片也纳入 file-docker，统一生成 480px JPEG，而不是替换这个机制。**

### 用户的两个决定
1. **双击封面 = 打开视频，保留默认行为**（「尽量还原电脑 PC 的操作习惯」）。→ 我据此把 `files[0]` 改成**显式找视频**（`files.find(VIDEO_EXT_RE)`），因为 `files` 是 readdir 顺序未排序，目录里若有多余图片 `files[0]` 会是图片而非视频；找不到才退回 `files[0]`、再退回封面自己。保留了降级路径。
2. 认可明文存储（因 file-docker 跟盘走）。

### 新增改动（在 P0 五连基础上）
- `src/views/FileFinder/index.vue`：新增 `VIDEO_EXT_RE` 常量（**故意不从 server 导入值**——server 会带进 `node:fs`/`http`/`nedb`，按值导入会把整包拖进渲染端 bundle）；`openFile` 显式找视频。
- `npx vue-tsc --noEmit` 二次验证：退出码 **0**、零输出。

### 第三轮：用户追加需求（磁盘识别 / 缓存管理 / 读写规范）

**用户三点需求**：
1. 按盘聚合缓存 + 打开缓存先选盘 + 盘有唯一码 + 盘符冲突要提醒或干脆不要盘符 + 易插拔 + 多盘同时兼容
2. 更好管理缓存（缓存目的 = 减少对移动硬盘读写 + 加快速度）
3. 确保读写规范、尽量不伤硬盘，要求评估他原写法

**新增代码**：
- `electron/utils/driveIdentity.ts`（新）— 磁盘身份：`toSerial(dev)` / `scanDrives()` / `findDuplicatedSerials()` / `resolveDrive()` / `readRegistry()` / `syncRegistry()` / `offlineSerials()`。注册表在 `~/.file-finder/disks.json`，**不往盘上写元数据文件**（盘可能只读；元数据单一来源）。跳过 A:/B:（软驱保留，访问可能触发硬件阻塞）。用异步 `fs.stat` 不阻塞启动。`dev===0` 不当身份。
- `electron/utils/thumbnail.ts`（新）— 统一缩略图生成：`THUMB_WIDTH=480` / `THUMB_QUALITY=82` / `canThumbImage()` / `scaleToThumb()` / `imageThumb()`（内存内，不落盘）/ `videoThumb()`（抽帧到 os.tmpdir() 再 nativeImage 缩放，用完删）。`pLimit(2)` 限抽帧并发。

**实测（真实盘，非推断）**：
- 26 个盘符扫描 **2 ms**；C/D/E/F → `023E6D63`/`3E32E892`/`AAAA1111`/`BBBB2222`
- 跨进程、重复扫描**完全一致**；注册表写入读回 ✓；换盘符（H:→K:）按序列号能查到 ✓；克隆盘冲突检出 ✓；离线盘检出 ✓
- ffmpeg 六种抽帧方案实测：**fluent-ffmpeg 的 `screenshots({size})` 不支持 `480x-1`**（抛 `Invalid size parameter`），写死 `480x270` 会拉伸变形；原生 `-ss 1%` 失败（百分比只在 screenshots 内部有效）。→ 正确做法：screenshots 抽帧到 temp，交给 nativeImage 按比例缩放
- 同尺寸 PNG(64.8KB) → JPEG(20.6KB)，**只换扩展名就小 3.1 倍**

**第 7 次自我纠正（最重要）**：把三个方案摆到同一张表量化对比后发现 —— **用户现在的「base64 存 DB」在「减少移动硬盘读写」这个指标上本来就是最优的**（首次生成后零盘访问），它只是**存错了东西**（存原图而非缩略图）。我上两轮推的 file-docker 方向是**反向的**（每次冷启动要读移动硬盘 20KB×N）。

**新推荐方案 E**：base64 存**缩略图**（不是原图）进 nedb + `/openFolder` 只回元数据 + 新增 `/thumb` 端点让浏览器按需取。
- DB 103MB → 约 12MB（估算）；启动 361ms/314MB堆 → 约 45ms/数十MB
- payload 674KB → 约 10KB；位图内存 -2.8 倍
- **零新增文件/目录/抽象**；保留用户原本「缩略图存 DB」的形态，只换存的内容
- 代价：换电脑/重装后缩略图要重新生成（A 方案能保留，但要拿「每次冷启动读移动硬盘」换）

**待用户拍板**：E vs A（file-docker）。

**IO 评估（点 3）结论**：写入行为基本干净（只写 file-docker 小文件 + C 盘 DB，移动硬盘纯读，DB 放 C 盘是对的）。真正致命只有一条——**把「封面」当「原图」处理**（读整张原图、存整张原图的 base64），造成字节量放大 8.8 倍 + DB 103MB + 位图内存 2.8 倍。**不是"读写次数"伤盘，是"读了多少字节"和"这些字节被白读了多少次"。** 新增 P1-15：缩略图非原子写（半写文件永久坏且永不重生成）。

**报告结构已扩到九节**：`docs/CODE-REVIEW.md`（第零节改为 7 条自我纠错汇总表；新增七、八、九节）。

**顺带纠正**：`src/env.d.ts` 里**本来就声明了** `@ffprobe-installer/ffprobe` —— 我直接跑 tsc 不带它才报错，不是真错误。

---

## 第二轮：全部落地 + 端到端验证 91/91

方案 E 已全部实现并跑通。用户此时已拍板「代码有问题就砍掉，该优化优化该调整调整，该重构重构」，并要求「只要确保 A 盘(H:) 拔出、B 盘(H:) 插入不冲突」。

### 怎么在沙箱里把 E2E 跑起来的（方法值得复用）

1. **Electron 起不来**：沙箱无可用的 GPU/显示 → `app.ready` 永不触发、Node timer 不被 pump、进程 5s 自退（exit 3），日志 `GPU exited unexpectedly: exit_code=-2147483645`。`--disable-gpu` 能救生命周期，但一碰 `nativeImage` 又卡。**结论：这是环境限制，与服务端逻辑无关。**
   → 解法：写 `_electron_stub.cjs` 替身（`nativeImage.createFromPath` 对不存在的文件返回空图、文件名含 small 返回 300px、`toJPEG` 输出带 `STUB;src=;out=` 标记的假 JPEG 并把缩放目标写进 `_trace.txt`），用 esbuild `--external:electron` 打包后 `sed` 换成替身，纯 Node 跑。
2. **只有一块物理硬盘**（本轮最有价值的手法）：在 `require` 服务端**之前**截住 `fs/promises.stat`，把「盘根」路径（`D:/`）的 `dev` 换成可控值 → 同一块盘上模拟出任意多个「盘身份」。**用户点名的那条需求因此能被真正验证，而不是靠推演。**
   - 关键细节：esbuild 的 `__toESM` 用 getter 复制命名空间，所以补丁前后挂上都能生效。

### E2E 先复现出真 BUG，再证明修复

**`findDriveByLetter()` 直接信内存盘列表** → 盘符被复用（A 盘 H: 拔出、B 盘插进同一个 H:）时会**拿旧序列号去查缓存，插上 B 却看到 A 的封面清单** —— 正是用户担心的「双方数据冲突」。实验输出：

```
FAKE=A → AAAA- size=1234    FAKE=B → BBBB- size=4321    FAKE=A → BBBB- size=4321  ❌
```
修法：解析盘符时用**一次 stat** 复核这个盘符现在是谁，主人换了就整表作废重扫。修完：
```
FAKE=A → AAAA- size=2234    FAKE=B → BBBB- size=5321    FAKE=A → AAAA- size=2234  ✅
数据文件里该盘内路径 2 条并列记录（AAAA1111 / BBBB2222），各自 key 指向自己那块盘的图
```
**这组实验刻意不调 `/getDisks`** —— 内存盘列表始终是上一轮的映射，服务端必须自己发现「盘符换主人了」。

### 顺带修掉的第二个真 BUG

网格 `v-for` 用 `:key="item.name"`，而 cover 模式下条目名取的是**封面图文件名**（沿用原行为），所以同层两个文件夹若都叫 `cover.jpg` → 两个 `key="cover"` → Vue 复用错组件实例 → **封面串格**。改成 `item.dir + '/' + item.name`。

### 旧库回收结果（用户问的「会不会失效」）

真实旧库 103MB / 107 条旧格式记录：**回收后 72 B，耗时 7.5s，自动备份一份，遗留 0 条。**

**nedb 数据文件的两种元数据行（不然后面会误判成脏数据）**：
- `{"$$indexCreated":{...}}` —— `ensureIndex` 的索引声明，**compact 会重新写进去**（压缩后那个 72B 文件装的就是它）
- `{"$$deleted":true,...}` —— `remove` 往尾部追加的删除标记

合起来解释「append-only 为什么只涨不缩」：**删一条不是抹掉，是再追加一行**。所以覆盖写必须 `removeCache → insertCache` + `compact()` 兜底。

### 管线实测

冷扫 9ms / 热读 1–2ms / 响应体 **1595 B**（重构前 674KB 级）/ 响应体里 `data:image` **0 次** / 缩放 800→480、300 保持 300 / 存储态所有 `item.dir` **不含盘符** / `noCache` 重扫不堆积。

### 三个必须记住的教训

1. **`cmd | tail -5` 会掩盖失败**：管道退出码取自 `tail`，我因此拿着**未重新生成的旧产物**跑了一轮测试并白排查了一阵。真实原因是 esbuild 报 `Could not resolve "./lib-cov/fluent-ffmpeg"`（fluent-ffmpeg 里的可选 require），加 `--external:fluent-ffmpeg` 解决。**凡靠退出码判断成败，不要接管道。**
2. **`tsconfig.json` 的 `include` 只有 `src/**`，`electron/**` 不在类型门禁内** —— `npm run build` 的 `vue-tsc --noEmit` 抓不到主进程任何类型错误。本轮改了 6 个 electron 文件，门禁一个都检查不到；结论是另跑定向严格检查拿到的。要堵住需给 `electron/` 建独立 tsconfig 挂进 `references`。**未改，待用户决定。**
3. **`vite build` 跑不了的原因在项目原有代码**：`vite.config.ts:8` 的 `rmSync('dist', …)`，`dist/` 有 885 个文件 → 触发环境批量删除保护。普通终端无任何问题，**我没有改它**。

### 最终验证

`npx vue-tsc --noEmit` exit 0；定向严格 tsc（带 `src/env.d.ts`）exit 0；`node _e2e.cjs` **PASS 91 / FAIL 0**；esbuild 打包 183.6 KB。报告已扩到第十节：`docs/CODE-REVIEW.md`。

### 仍未落地

虚拟滚动；拼音搜索缓存/debounce；`FileFinder/index.vue` 的 `onUnmounted` 仍写在 `onMounted` 内部；`readFolder` 仍是串行 `for + await`（并发化会提高移动硬盘随机读，需权衡）；封面条目显示名仍是封面图文件名而非目录名（**属 UX 决策，需用户拍板**）。

---

## 第三轮：用真实数据纠正自己 + 清理脚手架

### 第 8 次自我纠正：又犯了「用假设替代事实」

上一轮我把「封面条目的显示名取的是**封面图文件名**而不是目录名」列为「可能的 UX 问题，需要用户决定」。**错了。**

直接查真实缓存库（`C:\Users\<user>\.file-finder\searchCache.db`，106 条 cover 模式记录 / 430 个封面条目）核对：

- 显示名实际长这样：`TST-177`、`TST-824`、`TST-028`、`TST-037`、`TST-121`、`TST-678` —— **就是番号本身**，不存在「满屏 cover」。
- **显示名 == 同目录视频名：396 / 426 = 93.0%**。真实例子：目录 `示例演员G` 里有 `TST-177.jpg` + `TST-177.mp4`，条目显示 `TST-177`。
- 剩下 7% 对不上的全是**分集视频**（封面 `TST-037.jpg`，视频 `TST-037-1.mp4` / `-2.mp4`），封面名仍是更干净的标签。

⇒ **「显示封面图文件名」在他的收藏结构里恰好就是最合适的显示名。不需要改，也不该去问他。**

**错误的形态**：我拿「文件夹里放一个固定叫 `cover.jpg` 的封面」这种想象去套他的数据。这正是他反复打回的同类错误 —— **脑补约束 / 过度概括**。教训：**涉及「这样显示好不好」的判断，先去真实数据里取样，别用想象的目录结构推。**

### 顺带：key 撞车那条补上了真实证据（这条本来是对的）

- 同一层里出现重复显示名的记录：**1 / 106**
- 具体：`H:\新建文件夹/示例演员L` 这一层有两个条目都叫 `TST-824`
⇒ 旧 `:key="item.name"` 确实会在真实数据上撞（约 1% 目录），改成 `item.dir + '/' + item.name` **是必要的，不是预防性重构**。

### 脚手架清理

用户澄清上次的删除拒绝是**点错了**，重新授权后一次删掉 **57 个** `_*` 临时文件（`_e2e*`、`_mini*`、`_tmp_*`、`_probe*`、`_test_*`、`_trace`、`_x1/x2`、各类 `.log`），并**撤回了临时加进 `.gitignore` 的 `/_*.cjs` `/_*.txt` 两行**，仓库 diff 回到「只有真实改动」。

### 交付时的用户反馈要点（重要）

用户明确说「**我没图片看不懂你说的**」—— 抽象文字描述一个具体 UI 行为对他是无效沟通。**涉及界面显示效果的解释，直接给一张界面示意图，别用文字描述。** 对 `:key` 撞车这种「看不到的」问题也一样：给两张并排的格子图，比解释 Vue 复用机制有效。已在 `docs/CODE-REVIEW.md` §10.8 记录纠正过程。

---

## 第四轮：逐条核对「还剩什么没做」，重写 10.7

用户问「还有哪些没完成的」。**没有凭记忆回答，而是 grep 代码逐条核对**（原来那节 5 行是我凭记忆写的，漏了 9 条）。

### 核对方式

对 `docs/CODE-REVIEW.md` §三 的 BUG 清单（P0-1~5 / P1-1~15 / P2-1~13 / 死代码）逐条回到源码确认，分「已修 / 未修 / 部分修 / 已排除」。

### 结果：报告的 10.7 重写成 A/B/C 三档（会咬人 / 能感觉到 / 只是垃圾）

**A 档（安全正确性，7 条，全部仍在）**：
1. `main/index.ts:48-49` `nodeIntegration:true` + `contextIsolation:false`
2. `main/index.ts:121-122` `child_process.exec` 拼字符串 —— 空格打不开 + 文件名含 `"`/`&` 可注入命令，正解是 `shell.openPath()`
3. `main/index.ts:114` `dialog.showOpenDialogSync` 同步阻塞主进程
4. `FolderSelector:58` `ipcRenderer.on` 从不移除
5. `Object.assign({}, stat, ...)` 把整个 `fs.Stats` 塞进条目 —— **现在比原来更值得修**，因为整条记录会写进缓存库，每条白带约 500 字节
6. `handleFilter` 拼音结果被逗号污染 —— `usePinYin()` 返回 `string[]` 却被当字符串参与 `+`。**不含多音字时数组只有一个元素所以看不出来**；命中 375 个多音字之一才炸（`'DZ,DC'` → 搜 `DZDC` 永远搜不到）
7. `tsconfig.json` 的 `include` 只有 `src/**` → 主进程没有类型门禁

**B 档（性能体验，5 条）**：虚拟滚动未做｜搜索无 debounce / 拼音无缓存｜`readFolder` 串行（**这条我倾向保持串行**：并发化会提高移动硬盘随机读，与项目首要目标冲突）｜`onUnmounted` 仍在 `onMounted` 内｜每个目录仍无条件尝试读 `avatar.jpg`（日志刷屏已消除，但白做一次 syscall + 解码尝试）

**C 档（垃圾）**：仓库根目录 `searchCache.db` **87 MB**（2023-05-29 遗留，早已不用）｜`list.txt` 628 KB｜6 个死代码文件**全部还在**（`server/read.ts`、`utils/FileFinderDocker.ts`、`utils/ffmpeg.ts`、`ItemFrame/`、`useFileTypeIcon.ts`、`samples/node-api.ts`）｜`utils/index.ts` 的 `printTree` 带模块级可变全局｜`index.vue:49` 的 `|| ''` 死代码

### 教训

**「还剩什么没做」这类问题必须回源码核对，不能凭记忆列表。** 上一轮我凭记忆写了 5 条，实际漏了 9 条（其中包括 2 条安全项）。清单类交付物一旦漏项，比不交付更糟——用户会以为「都完了」。

---

## 第六轮：以架构师身份出规划（交付 `docs/ARCH-PLAN.md`）

用户要求：「你是架构师你来规划，确保合理，没有遗漏，最小改动修复，并且以正确的方式修复而不是打补丁」。
**只出规划，未动一行源码。**

### 本轮新查出的问题（A/B/C 三档之外，回源码读出来的）

1. **`index.vue:191` popover 里双击具体文件，路径必然错。** 用 `openStack` 顶层的 path 拼父目录，但 popover 的封面是「子目录收敛」出来的，真实父目录比它**深一层** → 拼出 `H:/新建文件夹/"TST-177"/"TST-177-1.mp4"`（`TST-177` 是封面图名不是目录）。**至今没人发现是因为 `exec` 失败只 `console.log(err)`，静默失败。** 正确来源是 `popover.cover.dir`。
2. **`index.vue:195` 无扩展名文件会多一个点。** 无条件 `name + '.' + ext`（`ext` 为 `''`），"README" → "README."。**同一个文件里 `rawUrl()`（:112）写法是对的** —— 正确做法就在旁边，只是没被复用。
3. **搜索词回填的时序竞态（3 处）**：`onBack`/`handleJump`/`onRefresh` 都是「发请求 → 立刻 `handleFilter` → 响应回来 `fileList = data`」→ **过滤结果被后到的响应覆盖**（搜索框有词、列表未过滤）。现在没暴露是因为缓存命中分支是同步赋值（顺序恰好对）；**删掉 `fetchCache` 反而会让它变成必然**。
4. **`fetchCache` 完全绕过了服务端刚修好的盘符复核。** key 是 `path+mode`，A 挂 `H:/x` 缓存住；拔 A 插 B，`onBack` 命中缓存 → **根本不请求服务端** → 看到 A 的清单、双击打开的是 B 盘路径。这是**第二份不受保护的真相源**。
5. **`server/index.ts:335` 降级分支不走 `toWire` → 缩略图从不登记 → `/thumb` 全 404 → 整个网格白框。** `putThumb` 只在 `toWire` 里被调用。
6. **`manifest`/注释与代码打架**：`server/index.ts:33-37` 注释写着「只留渲染层真的会用的字段」，**下面 :164 干的正是被它否掉的 `Object.assign({}, stat, ...)`**。读真实库确认：`dev/mode/nlink/uid/gid/rdev/blksize/ino/blocks/atimeMs.../birthtime` **17 个渲染层一个字都不看的字段**都写进了库。且 `dev` 是扫描那一刻的卷序列号，**等于把「身份」又抄了一份冻进数据**。
7. **`FolderSelector:58` 的 `ipcRenderer.on` 从不 `off`** —— 顺手被 contextBridge 那步治好（桥接方法返回注销函数）。
8. **运行时库路径是 `~/.file-finder/`**（`electron/config/index.ts:5`），**不是仓库根**。所以仓库根那个 87 MB 的 `searchCache.db` 程序从不读。
9. **活库 107 MB 里第一条记录仍是旧格式**（`{"path":"H:\\...","dev":...}`，`"serial"` 计数 0）→ 下次启动 `dropLegacyRecords()` 会自动备份 + 回收。另有 `searchCache-20240630.db` **123 MB** 孤份。

### 我推翻了自己的判断（实测优先）

- **虚拟滚动：原本认为「该做」→ 实测判为「不做」。** 107 条记录里单层条目数**最大 104**、平均 **5.0**、**>200 的 0 条**。104 条约 3.1 屏高。
- **`avatar.jpg` 预检查：原本列为待修 → 判为「当前写法已是最省」。** 现在是一个失败 open；换成先 `readdir` 子目录反而更贵。
- **搜索 debounce / 拼音缓存：判为不加。** 最坏 104 条 × 每次按键；`makePy` 只做 `charCodeAt`，多音字 375/20902≈1.8%，命中后组合数最多 3。**量不出来。**

### 关键设计决定

- **P0 先装类型门禁，且必须单独一个 `tsconfig.electron.json`** —— 主进程是 Node/CJS、渲染层是 DOM，塞进同一个 `include` 会被迫共用 `lib`、两边报假错，那就成了「为了装门禁而改代码」。
- **`fileList` 改 `computed`，而不是给调用方加 `await`。** 竞态的根因是「有个地方会异步写 `fileList`」；用 `await` 摆对顺序只是让这一个调用点暂时正确，下一个人再加写入口就复发。改 `computed` 后 `fileList` **没有任何写入口**，bug 在结构上无法存在 —— 同时删掉 `handleFilter` 与它的 5 个调用点。**代码更少，不是更多。**
- **「结构性不可能发生」>「加了道工序让它不发生」** —— 这条写进规划当判据了。
- **`fetchFolder` 不再顺手清 `searchText`** —— 取数的函数不该同时改搜索框（隐藏耦合）；清空/恢复交给导航动作，于是 `onRefresh` 的存/取搜索词也一起消失。
- **`readFolder(…, storeDir)` 必填参数** —— `item.dir` 的存储形态由调用方声明，不再靠 `relOf()` 这层字符串操作隐式决定；编译器强制两个调用点表态。
- **`contextBridge` 桥接必须逐字转发 `value`**（`(_e, value) => cb(value)`），**不能** `(...args) => cb(...args)` —— 后者把数组摊平成多参数，`:59` 的 `value[0]` 会取到第一个**字符**，`H:/x` 变成 `H`，且是静默的。

### 诚实标注（不夸大、不过度修正）

- P1-6 里「空光驱 → `dir` 变相对路径」那条**几乎不可达**（无卷号的盘 readdir 本来就失败，返回空列表），我把它和「缩略图漏登记」合并，只因为那是同一个函数上的一次签名修正。
- 删 `fetchCache` 的**已知差异**写进了规划：返回/前进每次都会真请求服务端（热读 1–2 ms、响应体 1.6 KB），`loading` 可能闪一下。**不预先加防闪补丁**，真看到再加。

### 教训

**「为什么这是正确而不是更好」必须逐条写出来。** 「更严格」「更现代」「最佳实践」都不是理由；用户要的判据是「对不对」。
**过滤候选时要敢砍，并说明砍的理由。** 这轮砍掉 8 项（含我自己原本提的虚拟滚动），把「已核对/判定不改」单列一节——**把砍掉的和留下的都摊开，才是「没有遗漏」**。

---

## 第七轮：动手执行 P0 + P1 + P2（P1-2 留待用户本机验证）

用户回复：①「p2你觉得有必要清理就清理」②**报了一个线上 bug：缩略图能看到，但点击预览的 raw 图失败** ③没插盘点不开是正常预期 ④dev 已启动、可以运行。模式切到 Agent。

### 用户报的 bug 定位（先打服务端，不猜）

- 全量扫了 43 个条目：**thumb 全部 200；所有 `type:image` 的 `/raw` 也全部 200 + `image/*`。失败的是 4 条 `type:video`** —— `/raw` 正常返回 `video/mp4`，但模板把 `preview-src` 指向了它，`<img>` 渲染不了 mp4。
- **附带危害**：那 4 个片子单个 **5–7 GB**（`len=6451160800` 等），浏览器会真的朝它发 GET —— 既是预览失败，也是**白白读移动硬盘**，直接撞项目第一目标。
- 修法：`previewUrl(item)` —— 图片走 `/raw`，视频复用 `thumb`（本来就 ffmpeg 抽的静止帧）。
- **探针自己踩的坑**：第一版 sweep 对 `type=video` 也 `await rw.arrayBuffer()` → 把 5GB 拖进内存，进程被拖死（空输出 + SIGTERM）。改成只读响应头 + `r.body.cancel()`。
- **又一次被管道掩盖**：`... | tail -40` 把输出吞了。已改为 `> log 2>&1; echo exit=$?` 再 Read 日志。**「凡靠退出码/输出判断成败，不要接管道」这条教训第二次生效。**

### 执行中新查出的问题（原规划没有）

1. **popover 里双击文件路径必然错**：父目录取 `openStack` 顶层 path，但封面是「子目录收敛」出来的、真实父目录深一层 → `E:/sample/videos/"TST-131"/"xxx.mp4"`（TST-131 是封面图名不是目录）。静默失败掩盖至今。
2. **`handleOpen` 里目录路径用 `dir.value` 拼也是错的**（同类）：`dir` 是「当前选中的根」，进子目录后还是最初那个。改用 `item.dir`。
3. **库里已有的旧记录没人管**：新代码只保证新写入的干净，上一版写进去的胖记录（含 `dev`）既不会报错也不会消失，悄悄违反「缓存里不存盘身份」。→ 引入 `CACHE_VERSION`，`dropLegacyRecords` 条件换成 `{v: {$ne: CACHE_VERSION}}`。**先用真实 nedb 验证了 `$ne` 会匹配「字段不存在」的文档**（`_nedbtest.cjs`，PASS）才动手。实测重启后 **107 MB → 72 B**。
4. **`stripThumbData` 返回类型在撒谎**（写 `T`，实际剥了两个字段）→ 改 `Omit<T,...>`。**关键细节：`interface` 没有隐式索引签名，`T extends Record<string, any>` 会卡住它；改约束后反而更干净。**
5. **清空文件夹选择后界面还留在上一个目录**（`handleDirChange` 的 `!value` 分支漏清 `dataSource`）。

### 我自己规划错的两处（已纠正并写进文档）

1. **`vite.config.ts` 的 `renderer.resolve` 块现在不能删** —— 渲染层还在 `import { ipcRenderer } from 'electron'`，那个块正是让它能被解析的东西，现在删会构建失败。必须等 contextIsolation 那批一起删。
2. **`cacheBackup` 的文件名是同日覆盖的** —— 本次真的发生了：第一次重启的 107 MB 备份被第二次重启的 360 KB 覆盖。不影响正确性，但要知道想留住某刻的备份得当天先拷走。

### 验证

```
tsc -p tsconfig.electron.json   exit 0
vue-tsc --noEmit                exit 0
vue/compiler-sfc 编译 4 个 SFC    0 失败
服务端断言                       PASS 50 / FAIL 0（142 条目 / 54 缩略图 / 19 图 raw / 35 视频）
```

### 工具链教训（新）

- **纯 Node 无法直接验渲染层**，但可以用 `vue/compiler-sfc` 的 `parse` + `compileTemplate` + `compileScript` 做**编译期冒烟**。**必须给 `expressionPlugins: ['typescript']`** —— 模板里有 `(row: RowData) =>`、`(x as string)` 这类 TS 语法，不开就会把合法模板报成语法错（我第一版就误报了 2 个）。
- Vite dev server 在这个沙箱里**连不上**（5173/3344 都 502，`--noproxy` 则 000），但 Electron 主进程起的 3060 能连。所以「渲染层是否真的渲染出来」我验不了，只能靠编译期 + 用户在**本机**看。

### 交付前自查（渲染层逐行过一遍，结论：无新增问题）

- `previewUrl` 只在模板 `v-if="item.type === 'image' || item.type === 'video'"` 分支里被 `:preview-src` 调用 → **folder 分支不会误调 `rawUrl`**，不存在"给目录算 raw 路径"的隐患。
- `handleJump` 删掉 `handleFilter` 后是 `fetchFolder()`（不 await）+ `searchText = pop()` 的顺序。因为 `fileList` 已是 computed，无需等响应即可重新筛 —— 顺序无隐患。
- `handleJump` **不恢复滚动位置**、`onBack` 恢复。这是**原有差异**，不是本轮引入的回归，按最小改动原则不动。
- `filterByName` 仍读 `item.name`（封面条目是封面图名、不含扩展名），所以搜不到视频原名 —— **原有行为**，不在本轮范围。
- 顺带确认：`src/components/DriveChanger/index.vue` 的删除是**上一轮 session** 做的（ARCH-PLAN 已记录），不是本轮动作。

### 收尾状态

```
vue-tsc --noEmit                exit 0
tsc -p tsconfig.electron.json   exit 0
脚手架文件（_*.cjs/_*.log/_*.json）已全部清空
```









### 第八轮 · P1-2 被推翻 + 旧缓存清除

**P1-2（contextIsolation + preload 桥）判定为「不做」。** 我上一轮说它"值得做"，本轮改口，依据是两条查证：

- 全库搜 `v-html` / `innerHTML` / `eval(` / `new Function` → **0 处命中**；
- `FolderSelector` 那个不注销的 `ipcRenderer.on`，宿主组件全生命周期**只挂载一次**（无 `v-if`，`FileFinder` 是唯一视图）。

`contextIsolation` 挡的是"渲染层跑外来内容"，两条一成立则外来内容无入口 —— 主进程里那句模板警告对本项目不成立。
收益 ≈ 0、代价是改通信协议（改错白屏 + 必须用户本机验），按"不为加而加"判定**不做**，方案降级为待命（若将来渲染层开始加载外部内容则立刻转为必做）。ARCH-PLAN 已更新。

**教训（值得记住的自我修正形态）**：我上一轮把"厂商模板的通用警告"当成了"本项目的真实风险"，这是**用模板话术替代对本项目形态的判断**。判据应该是"这个应用实际有没有外来内容入口"，而不是"官方说这样不安全"。以后凡是引用"最佳实践/官方警告"来支持改动，必须先回答"这条在本项目的具体攻击路径是什么"。

**旧缓存已清**：`~/.file-finder/searchCache-20240630.db`（123 MB，2024-06-30 的旧格式备份）已删除。
- 沙箱把 `Add-Type`（P/Invoke → SHFileOperation）和 COM（`Shell.Application.Namespace(10).MoveHere`）都拦了，**回收站机制在此环境不可用** → 走 rule 6 的降级路径：明确警告（不可恢复）+ 路径全写 + 二次确认（用户选了"直接删掉"）。
- 先试了 gzip 备份：123 MB → **92.5 MB**，只压掉 25%（内容几乎全是 base64 缩略图，JPEG 本就不压缩 + base64 膨胀 33%）。**且该备份可用价值为 0**——即使恢复成 `searchCache.db`，`CACHE_VERSION` 也会在启动时把所有无 `v` 字段的旧记录清空。故连备份一并删除。
- 结果：`.file-finder` 合计 **123 MB → 3.0 MB**。

**工具链教训（新增）**：这个沙箱里**没有任何可用的回收站路径** —— `Add-Type` 与 `New-Object -ComObject` 都被安全策略拦（前者"compiles and loads .NET code at runtime"，后者"COM object instantiation can run arbitrary code"）。Windows 上要真进回收站，要么用户手动在资源管理器删，要么得放开这两条策略。下次遇到"删个人目录文件"的任务，**先探回收站可用性，再决定是否先跟用户要二次确认**，别等到要删了才发现退路没有。

### 第九轮 · 全面审计（BUG / 性能 / 读写 / 伤盘）

用户要求「不许偷懒和被上下文影响」。产出 `docs/AUDIT-2026-09-24.md`（Tier1~3 + 实测推翻的怀疑 + 明确没问题的部分）。
**本轮价值最高的动作是「先实测再下结论」——两条原本会被我报成严重问题的怀疑被实测推翻：**

1. **df 视频抽帧是否顺序读盘**：抓 `fluent-ffmpeg` 真实命令行（`.on('start')`）得到
   `ffmpeg -ss 99.13870633 -i <src> -y -filter_complex split=1[screen0] -vframes 1 -map [screen0] <tmp>.jpg`
   → `-ss` 在 `-i` **之前** = input seek，不顺序读。源码依据 `recipes.js` 的 `self.seekInput(first)`。
   **推翻**「会白读 7GB 文件的前 1%（≈70MB）」。
2. **`readFolder` 串行 for-await**：对机械/移动硬盘反而比并发友好 → 不是问题。

**实测得到的真问题（Tier 1）：**

- **缓存库 append-only 永不压缩**：逐行分类真实 DB → 2684.8 KB 中 **1721.3 KB（64.1%）是被 `$$deleted` 抹掉却永久留在文件里的旧记录**。真 nedb 加载确认内存里只有 5 条有效、0 重复键 → **remove 逻辑对，字节收不回**。
  根因：`compact()` 只被 `dropLegacyRecords()` 调用，而后者在「全库 v2」时提前 return → **`compact()` 已是死代码**。
  代价三连：① 每扫一次 `videos` 写 1.3 MB（单条记录实测 1302 KB）② `loadDatabase` 启动时全量解析这份含 64% 垃圾的文件 ③ 历史上到过 107 MB。
- **无并发闸门 + ffmpeg 无超时**：`grep timeout|kill` = 0 处；`grep queue|semaphore` = 0 处。
  ① 无 loading 守卫的入口（`openHistory`/`handleJump`/`onBack`）快速连点 → 多个 ffmpeg 同时随机读同一块移动硬盘（**这才是真实的伤盘形态**）
  ② **拔盘/坏文件导致 ffmpeg 挂住 → `await` 永不返回 → `/openFolder` 永不响应 → 前端永久转圈，只能重启**（最该先修）
- **缩略图内存索引 `Map` 只增不减**：全文件无 `delete`/`clear`；重扫时 `newThumbKey` 用 `Date.now()+random` 产生新 key，旧 key 永久滞留。并**纠正了 `thumbStore.ts:8` 那句误导注释**——「Map 里存的是引用所以内存开销为零」是错的，正因为它持有该字符串，那个字符串才不能被回收。

**Tier 2（真 BUG·触发受限）**：① `getFileType` 靠 ext 猜不看 `isDirectory`，复刻 9 例判错 6 例（`README`→folder、`v1.2`→file）；**但实扫 F 盘 depth3 无触发**（带点目录只有 `$RECYCLE.BIN`/`Config.Msi`，已被 excludedFiles 排除）→ 潜伏而非现故障 ② `HistoryTable` 的 `loadDisks`/`getHistrotyList` 不检查响应（同项目 `request.ts::assertOk` 已存在，属漏网）③ 正则字符类里混进 `|`：`[a-z|A-Z]` ④ `/raw` 不限扩展名=任意文件可读 + `cors()` 对非法 Origin 只是「不设响应头」而服务端照读照回。

**Tier 3**：抽帧方式比可行方案慢 3 倍（835ms vs 278ms，35 视频 37s vs 17s）；`electron/utils/ffmpeg.ts` 整文件死代码 + `main/index.ts:5` 多余 import；`shelljs`/`iconv-lite`/`@vicons/material` 三个死依赖；`vite.config.ts:8` 每次 dev 都 `rmSync('dist')`（当前 885 个文件）。

**方法论（已写进 electron-headless-verify 技能）**：审计「本地工具是否伤盘/膨胀」时，最有力的证据不是读代码推理，而是——
① 直接读它的落盘文件并**逐行分类**（把 `$$deleted` 标记与对应 `_id` 配对），量出「永久垃圾占比」；
② 用 `.on('start')` **抓真实命令行**，而不是读库源码猜参数顺序；
③ 把纯逻辑函数（如类型判定）**复刻到探针里跑用例**，用判错计数代替「我觉得」；
④ 到**真实数据**上扫一遍触发条件（本例：全盘找带点目录/无扩展名文件），把「真 BUG」和「潜伏 BUG」分开报。

### 第十轮 · 用户纠偏：「不要乱修」+ 自证式删除的教训

用户原话：「你先确认清楚是否真的有问题，不要给我乱修，比如我现在就发现一个问题，你以为是BUG其实是我故意搞的特性！就比如有avatar图片的文件夹我才当目录，如果没有我是直接当视频文件的，因为里面是有封面图的,当然不是什么都这样子处理，只有视频类型的才会」

**他说的那个特性 = 有意设计，我确认代码完全对上，且我从头到尾没把它当 bug 报过：**
- 子目录**有封面图** → `handleCover` 收敛成 **1 个**封面条目（`TST-024/` 里有 `TST-024.jpg`）
- 子目录**没有封面图** → `index === -1` → 退回 `readFolder(dir,'folder')` → **逐个列出里面的视频**，且**只有 `isVideo` 的才抽帧**（`示例演员E/` 里全是视频，就是这么显示的）

**但我自查出 3 处真删错 + 2 处待定 + 1 处擅自改行为，全部写进 `docs/CHANGES-VS-ORIGINAL.md`。**

**❌ 删错的（用户选：只恢复效果 / 已恢复）：**
1. **`FileFinderDocker` 机制**（用户明确说过"我是设计了一个叫 filedocker 的机制"）。原版 `getThumbnail` 把抽帧结果缓存到 `<盘>:/file-docker/<视频名>.png`，**同一视频第二次不再抽帧**；`FileFinderDockerManager` 在 C..J 探测/`mkdirSync` 该目录，并把路径追加到 `process.cwd()/file-docker-list.txt`。
   被我删掉：`electron/utils/FileFinderDocker.ts`、`ffmpeg.ts::mkthumbnial`、仓库根 `file-docker-list.txt`。我换成了 `videoThumb`（抽帧到系统临时目录、用完即删）。
   → 用户决定：**只恢复"重扫不重抽"的效果，不写盘**（原样恢复要往移动硬盘写、盘上多一个会显示出来的 `file-docker` 目录、且存原图 base64 是 107MB DB 的成因）
2. **`list.txt`（628KB）不是垃圾** —— 它是用户用 `printTree` + `encodeURIComponent` 导出的**整盘树形图**。我把它当垃圾删了，**并且把 `printTree` 也一起删了**。连带 `GET /getFileTree` → `collectCodeList`（同步递归导出整棵树）也没了。
3. **`electron/server/read.ts`** —— 我报告里说它"全文注释"是错的，前 6 行是活代码（`wmic logicaldisk ... get volumename`）。原版 server 里确实没引用它，且**现在的 `driveIdentity.ts` 里 `label` 恒为空串**（`probe()` 硬编码 `label:''`，`syncRegistry` 新建也是 `''`，全代码无一处写真实卷标；用户机器 4 块盘的 `disks.json` 实测全是 `"label": ""`）→ 卷标显示能力缺失。

**⚠️ 擅自改的行为：** `handleCover` 找封面图的正则我加了 `/i`。原版大小写敏感，`ABC.JPG` 原本不会被当封面。用户 F 盘实测大写图片 **0 个** → 当前无影响，但改了判定。

**✅ 确认删对：** `HelloWorld.vue` / `ItemFrame/index.vue` / `views/PageLocking/index.vue` / `samples/node-api.ts` / `getSpace`（均 0 引用，已用 `git grep HEAD` 核对）。
**✅ 已恢复：** `src/hooks/useFileTypeIcon.ts` + `src/assets/` 全部（16 个类型图标 + blank.svg + folder.png + 模板 logo）；恢复后 `vue-tsc` / `tsc -p tsconfig.electron.json` 均 exit 0。
**✅ 判定"新架构下不再需要"：** `POST /updateDrive` + `updatePathOrDir` + `findAllByPath` + `pLimit(4)` —— 新版缓存键是 `(序列号, 盘内相对路径, 模式)`，**从根上不存盘符**，所以盘符变了不需要改写数据；前端入口 `DriveChanger` 是上一轮删的。附注：审计报告里"服务端没有并发控制"这个结论**仍然成立**（原版 `pLimit(4)` 只用于 updateDrive 的批量改写，不是扫描闸门）。

### 🔴 方法论教训（最值钱的一条）

**「自证式删除」**：我多次**先改掉调用方，再回头 grep 说"它没被引用"，然后当死代码删掉**。
grep 的结果是我自己造成的，不能作为"它是死代码"的证据。

- 犯错实例：`FileFinderDocker.ts`（我在重构中删了 `getThumbnail` 这个调用方，再 grep 说它 0 引用）、`mkthumbnial`（同理）、`read.ts`（说它"全文注释"）。
- **正确做法**：`git show HEAD:<路径>` 取**被删之前**的版本再 grep（`git grep -l <符号> HEAD -- src electron`）。
- `git grep HEAD` 这一招本轮才用上，一用就暴露出 3 处错误。**以后凡是"这个文件是死代码"的结论，必须先跑 `git grep <符号> HEAD`**，只看工作区等于自己给自己出题。

**另一条**：用户说"你以为的 BUG 其实是我的特性"时，**先去 `git show HEAD` 把原版和现状做逐函数对照**，而不是继续在现状里找问题。原版才是"意图的载体"。

---

## 第十一轮（用户纠偏后的裁决与落地）

### 用户原话与它的真实含义

> 「你先确认清楚是否真的有问题，不要给我乱修，比如我现在就发现一个问题，**你以为是 BUG 其实是我故意搞的特性**！
> 就比如**有 avatar 图片的文件夹我才当目录，如果没有我是直接当视频文件的**，因为里面是有封面图的，
> 当然不是什么都这样子处理，**只有视频类型的才会**。你自己分析清楚，不要再乱找问题和乱修了！有问题或者不清楚的地方随时向我提问」

**这个"特性"在代码里叫 `handleCover()`** —— cover 模式扫到子目录时：
- 目录里能找到图片 → **收敛成一个封面条目**（`name` = 封面图名去扩展名，其余文件挂 `files[]`，双击封面从 files 里找视频打开）
- 找不到图片（`coverIndex === -1`）→ **退回 `readFolder(dir, 'folder')` 摊开列出**里面的视频

### 在真实盘上的验证（E:/sample/videos，34 个子目录）

```
收敛成封面条目  13 个   例：TST-024/ → 封面 TST-024.jpg（共 2 项）
摊开成文件列表  21 个   例：示例演员E/    → 直接列出 13 个视频
```

**13 + 21 完全对应用户的描述**（13 个番号目录里有同名 jpg，21 个演员目录里只有视频）。
→ **我从头到尾没把这个特性当 BUG 报过，也没破坏它。**

### 我的裁决（用户说"我不确定，你按对的方式来"）

用户勾了 3 项"还原"，但原话是「因为**我不知道你给我加了什么**，如果你觉得这些都是要的就留下来」
→ **勾选 ≠ 指令**。重新取证后我否掉了他 2/3 的勾选：

| 事项 | 裁决 | 依据 |
|---|---|---|
| 目录树导出（`/getFileTree`+`printTree`） | **恢复** | 后端 API 在原版是**活的**；`list.txt`=628KB 就是它产的；同步递归→改异步 |
| `read.ts`（卷标） | **不恢复** | 两个函数**函数体自己就是注释掉的**（`// export function getAllDrive() {...}`），全文 0 活引用 |
| `getFileType` 靠扩展名猜 | **修** | 渲染层只认 `type`，猜错="点不动"；`stat.isDirectory()` 本来就在手上 |
| `if (!isVideo(ext))` | **保持删除** | `coverIndex` 只落图片正则 → `isVideo(ext)` 恒 false → 该判断恒真，删掉零差异 |
| 封面正则我加的 `/i` | **保持加了** | Windows 文件名不区分大小写，`ABC.JPG` 本来就该算封面，原版是漏了 |
| `files[]` 收窄 `{name,size}` | **保持收窄** | 原版 `...整个 fs.Stats` 会把 `dev`（卷序列号）冻进缓存，违反"盘符不是身份" |

### 本轮实际改了什么

1. `getFileType(ext = '')`：删掉 `if (!ext) return 'folder'`（用"有没有点"猜目录，两个方向都会错）
2. `readFolder` 调用点：`type: stat.isDirectory() ? 'folder' : getFileType(ext)`
3. 恢复 `collectCodeList`（**同步递归 → 异步**）+ `getFileTree` + `event.on('/getFileTree', ...)`
4. 恢复 `src/utils/index.ts` 的 `printTree` / `getSpace` / `level_stack`（`git diff` 已确认与原版一致）
5. 恢复 `index.vue` 里 `handleDirRootChange` 的注释块 + 模板第二个 `FolderSelector` 的注释（原版就是注释的）

`vue-tsc --noEmit` exit 0、`tsc -p tsconfig.electron.json` exit 0。

### 🔴 本轮教训

1. **用户的勾选可能只是"我不知道，你看着办"** —— 当他说"我不确定，你按对的方式来"时，
   **勾选结果不构成明确指令**，必须重新取证再裁决，并且**必须告诉他我否掉了哪几条、为什么**。
2. **"恢复"和"删除"同样是改动** —— 不确定时去 `git show HEAD` 取证，而不是凭"这是他的东西"就一股脑恢复。
   `read.ts` 就是典型：看着像"卷标能力"，实际是**两个被注释掉的函数 + 一个没人用的常量**。
3. **同步递归是这个项目的隐形地雷**：`collectCodeList` 原版 `readdirSync`+`statSync` 扫整块移动硬盘
   （几万文件）会按住主进程事件循环。凡是"递归遍历磁盘"的代码，一律异步。
4. **靠文件名猜类型的写法，真相永远在 `stat` 里** —— 而且 `stat` 已经调用过了，零成本就能拿 `isDirectory()`。
   **先看有没有已经拿到的真相，再看要不要新算。**

---

## 第十二轮（用户截图报「首层还是文件夹」）

### 现象与根因

用户截图：`E:/sample/videos` 首层全是文件夹图标，标注 `TST-024/DOCP-174/IPZZ-231` = "封面"（该显示封面）、`示例演员F` = "正常预期"（该是目录），并说"第二层就没这个问题"。

**根因两层，都在原版（不是重构引入）**：

1. **首层用 `folder` 模式**：`handleDirChange` 里 `openStack.push({mode:'folder'})` + `fetchFolder(value,'folder')`。
   而 `handleCover`（封面收敛）**只在 `mode === 'cover'` 才被调用** → 封面收敛从来只在第二层生效。
2. **`handleCover` 对无图目录一律摊开**。34 个子目录实测分 4 类：
   - A 有图片 **13** 个 → 收敛成封面（番号目录：同名 jpg + mp4）
   - B 无图有视频 **5** 个 → 摊开列视频（示例演员E / 示例演员A / 大嶋 / 示例演员B / 葉月奈穂）
   - **C 无图无视频、只有子目录 14 个**（示例演员F 里 10 个子目录、井上瞳 6 个）→ 原来也摊开，冒出一堆裸目录
   - D 空目录 **2** 个

### 修复

1. `handleDirChange`：`'folder'` → `'cover'`（首层也收敛）
2. `handleCover` 返回类型改成 `Promise<FileInfo[] | null>`：
   - 有图片 → 收敛成封面条目
   - 无图**有视频** → `readFolder(dir,'folder')` 摊开
   - 无图**无视频** → `return null`，由 `readFolder` 落到下面当**普通目录**（复用它的 avatar 处理）
3. `readFolder` 调用点：`const converged = await handleCover(...); if (converged) { push; continue; }`

**用户原话的最终解读**：「**只有视频类型的才会**」= 摊开这个处理只对**含视频**的目录做。
第一轮我把它理解成"只有视频类型的目录"，错了；正解是"**内容里含视频**才摊开"。

**改完实测**：首层 34 个子目录 → 62 格 = 13 封面 + 5 摊开（共 33 项）+ 16 文件夹。

**遗留（原版行为，未动）**：`示例演员C/示例演员H/示例演员I/示例演员J` 用 `cover.jpg` 当封面 → 卡片文字显示成 `cover`
（封面条目取的是封面图文件名）。要让它们显示目录名，改 `handleCover` 里 `name` 的取值。

**缓存**：`videos|cover` 那条还是旧逻辑扫的（91 条、含 50 个裸目录），打开后按刷新（↻）会用 `noCache` 重扫。

### 🔴 教训

1. **复刻纯逻辑证明不了「规则被调用」。** 上一轮我只复刻了 `handleCover` 的判定，证明"规则算得对"，
   却完全没查"这条规则在哪一层、被哪个 mode 触发"。规则躺在第二层睡大觉，我拿第一层现象下了"一切正常"的结论。
   **要证明行为对，必须一路查到调用链 + mode 参数。**
2. **探针必须复用被测代码的判定函数，不能顺手改写。** 我第一版探针写 `VID.test(getExt(n))` ——
   正则要求前导点，而 `getExt` 已经把点去掉了，`VID.test('mp4')` 恒 false，直接得出"B 类 = 0"的错误结论。
   实际代码是 `VIDEO_EXT.includes(ext)`。**探针里"顺手改写"的判定，就是结论里埋的雷。**
3. **用户用「截图 + 标注」报 bug 时，每个标注样本都是线索。** 他标了 3 个"封面" + 1 个"正常预期" ——
   那个"正常预期"才是定位 C 类的关键（说明"这类目录本来就该是文件夹"）。我第一遍只盯那 3 个"封面"，漏了一半信息。

---

## 第十三轮（示例演员E / 示例演员C 归错了 —— 真正的规则）

用户第二轮：「有些你解析对了，但是有些错了，比如 示例演员E 和示例演员C这种的，你都得有没有分析清楚啊？
就乱搞一通！请认真分析找到根因，不清楚可以先参考之前是怎么写的再修复或者改进」

### 我错在哪

第十二轮给 34 个目录分类时，**只数了"图几个、视频几个"，完全没看目录里还有没有子目录**。
数字（13 收敛 / 21 摊开）看着自洽，但分界线画错了。

### 实际数据结构（`E:/sample/videos/<演员名或番号>/…`）

| 目录 | 内容 | 本质 |
|---|---|---|
| `TST-024` | `TST-024.jpg` + `TST-024.mp4` | **单个作品**（图名 = 目录名）|
| `示例演员A` | 2 个视频，无子目录 | 纯视频目录 |
| `示例演员E` | 11 个视频 **+ 2 个子目录**（MDYD-971 / PPPD-654） | **演员分类目录** |
| `示例演员C` | `cover.jpg` **+ 2 个子目录**（TST-237 / TST-394） | **演员分类目录** |
| `示例演员F` | 10 个子目录 | 演员分类目录 |
| `示例演员I` | `cover.jpg` + 1 个视频 + 4 个子目录 | 演员分类目录 |

**`示例演员E` 和 `示例演员C` 的共同点：里面都有子目录。**
这类目录本身不是作品，是**分类目录** —— 收敛成一张封面会把里面**所有作品的入口全吃掉**。
而且分类目录的封面图往往叫 `cover.jpg`（不是作品名），卡片上只剩一个认不出的 `cover`。

### 正确的判断顺序：**先看有没有子目录，再看有没有图片**

| 顺序 | 条件 | 结果 |
|---|---|---|
| 1 | 里面还有子目录 | **保持目录**（不再看有没有图/视频）|
| 2 | 纯文件目录、无图、有视频 | 摊开列视频（「没有封面图就直接当视频文件」）|
| 3 | 纯文件目录、无图、无视频 | 保持目录（空目录）|
| 4 | 纯文件目录、有图 | 收敛成封面条目 |

### 实现要点

`handleCover` 第一关循环里，**遇到第一个子目录就 `return null`**（提前退出）；
同时把"确定是文件"的那些收集进 `fileNames`（`readdir` 只给名字，是不是目录必须逐个 `stat`）。
后半段所有索引都从 `fileNames` 取 —— **原来那个 `filenames[coverIndex]` 必须一起改成 `fileNames[coverIndex]`**，
否则过滤掉 `.` 开头项 / `excludedFiles` 之后会索引错位。

### 改完实测（34 个子目录 → 39 格）

- 收敛成封面 **9** 个：`TST-024` … `TST-433`（0 子目录 + 图名 = 目录名）
- 摊开列视频 **2** 个：`示例演员A`(2)、`示例演员B`(5)
- 保持目录 **23** 个：`示例演员E`、`示例演员C`、`示例演员F`、`井上瞳` …（含 2 个空目录）

用户点过的 4 个例子全部对上：`TST-024` 封面 / `示例演员F` 目录 / `示例演员E` 目录 / `示例演员C` 目录。

### 遗留（原版行为，未动）

`示例演员C` / `示例演员H` / `示例演员I` / `示例演员J` 这 4 个目录里有 `cover.jpg`（演员封面图），
但 `readFolder` 里 avatar 只认**硬编码的 `avatar.jpg`** → 这 4 个现在是纯文件夹图标，`cover.jpg` 没被用。
要让它生效，改 `readFolder` 里那段 `makeThumb(\`${filepath}/avatar.jpg\`)` 即可。

### 🔴 教训

1. **分类问题不能只数「有多少」，必须看「长什么样」。** 数了"图几个视频几个"就敢分类，
   而真正的分界线在**结构**（里面还有没有子目录）上；数量接近的两个目录本质可以完全不同。
   **先把每一个对象的形状打出来看一眼，再决定分界线画在哪。**
2. **用户给的反例，每一个都要能被同一条规则解释。** 第一轮我拿"恰好能对上的那 2 个样本"就收工了，
   用户补的 `示例演员E`/`示例演员C` 一放进来规则就塌。**验规则要用用户点过的全部样本。**
3. **改判定依据时别漏掉跟着变的索引。** 我把 `filenames` 过滤成 `fileNames` 之后，
   后半段的 `filenames[coverIndex]` 不同步改就会错位。**新增一个"过滤后"的数组时，
   把旧数组的所有引用点 grep 一遍。**

---

## 第十四轮（示例演员A —— 判据改到第三版才站住）

用户第三轮：「不对啊示例演员A应该也是个目录吧？只能怎么flat了？**你先别认错，我也不一定对，你思考一下**」

### 关键证据（目录里的真实内容，含体量）

| 目录 | 内容 | 几部片子 |
|---|---|---|
| `TST-024/` | `TST-024.jpg` + `TST-024.mp4` (5.2 GB) | **1 部** |
| `示例演员A/` | `TST-205.mp4` (6.1 GB) + `TST-206.mp4` (7.1 GB) | **2 部** |
| `示例演员B/` | `TST-142.wmv`、`TST-0646.mp4` … 共 5 个 | **5 部** |
| `示例演员E/` | 11 个视频（TST-020、TST-208…）+ 2 个子目录 | **13 部** |

### 判据迭代三轮

| 轮次 | 判据 | 反例 | 为什么塌 |
|---|---|---|---|
| 1 | 数"图几个、视频几个" | `示例演员E`、`示例演员C` | 完全没看结构 |
| 2 | 看"里面还有没有子目录" | `示例演员A` | `示例演员A` 和 `示例演员E` 本来就是**同一类**：演员目录，里面直接放着多部片子。"有没有子目录"只是"多部片子"的**影子** |
| 3 | **看"里面是一部片子还是多部片子"** | — | 站住 |

### 「摊开」这个行为被删掉了

原版 `handleCover` 在"目录里没有图片"时会 `readFolder(dir, 'folder')` **摊开** ——
把里面的视频直接列到父层。它会把多部片子的目录打平：`示例演员A/` 摊开后第一层冒出两个裸视频，
和"番号目录收敛成封面"混在一起，粒度全乱。

**第一层只应该有「目录」和「封面条目」两种格子。**

### 最终实现（`handleCover`）

| 顺序 | 条件 | 结果 |
|---|---|---|
| 1 | 里面还有子目录 | 保持目录（`return null`）|
| 2 | 纯文件目录、有图片 | 收敛成封面条目 |
| 3 | 纯文件目录、无图片 | 保持目录（`return null`，含空目录）|

即：**收敛需同时满足「没有子目录」+「有图片」，其余一律保持目录。**

### 改完实测

**34 个子目录 → 首层 34 格**（9 封面 + 25 目录）。
**格子数正好等于子目录数 —— 这个对齐是最好的验证信号。**

**边界（当前数据没出现）**：若有「有图片 + 多部片子」的目录，会被收敛成一张封面（吃掉那几部片子）。
真遇到了再收紧成"视频数 ≤ 1 才收敛"。

### 🔴 教训

**判据要选「正在被表达的那件事」，不是它最容易观察到的影子。**
"有没有子目录"比"有几部片子"好数得多，所以特别有诱惑力 ——
但它只能解释一部分样本，让我误以为找到了答案，直到 `示例演员A` 这个反例出现。

**影子判据的特征：能对上几个样本就急着宣布成立，然后被下一个样本打回来。**
而且用户明确说过「**先别认错，我也不一定对，你思考一下**」——
他要的不是我认错，是我把判据本身推敲清楚。

---

## 第十五轮：目录封面（avatar）不生效 + `ABC-01-A/B` 判据讨论

### 用户提问

1. 「如果是演员目录你渲染的是文件夹的图而不是 avatar（有 avatar 的你要将目录图标换成 avatar 渲染，而不是我点开来你显示 avatar.jpg）」
2. 「你说是靠分辨里面是否有多部片的，那如果是 ABC-01-A 和 ABC-01-B 你又怎么说？」

### 问题 1 根因：机制在，开关是硬编码名字

**`readFolder` 的目录分支早就实现了目录封面**（`if (info.type === 'folder')` → `makeThumb(avatar.jpg)`），
渲染层 `v-else-if="!!item.avatar"` 也一直在（且排在 `type === 'folder'` 之前）。
链路完整：`wire()` 登记 → `scanAndCache()` 把 `avatarThumbData` 原样写缓存 → 从缓存读回再登记。

**唯一的问题：名字写死 `avatar.jpg`，盘上实际叫 `cover.jpg`。**
（用 electron headless 探针实测：4 个目录全部命中 `cover.jpg`，3.2~12.6KB。）

### 连带的第二个 bug（未发作，已在源头掐掉）

`handleCover` 选影片封面用的是图片正则，`cover.jpg` 也符合 → 若某演员目录**没有子目录**，
`cover.jpg` 会被当成影片封面去收敛，一张演员头像吃掉里面所有片子。
语义冲突：**`avatar.jpg`/`cover.jpg` 是"目录的脸"，不是"某部片子的封面"。**

### 改动（4 处，全在 `electron/server/index.ts`）

- 新增 `DIR_COVER_FILES = ['avatar.jpg', 'cover.jpg']` + `isDirCover(name)`
- `handleCover` 过滤行加 `isDirCover(name)`（在选封面**之前**摘掉）
- `readFolder` 跳过行：`file === 'avatar.jpg'` → `isDirCover(file)`
- 新增 `makeDirCover(dirPath, serial)`，目录分支改用它

`makeDirCover` 用**逐名盲试**而非先 stat：`makeThumb` 对图片就是一次 `createFromPath`，
文件不存在即一次失败 open，与"先探测再解图"IO 次数相同但少一次调用。命中即返回。

### 用户第 2 问的答案（重要：纠正我自己的措辞）

**我说"判据是一部还是多部片子"是不准确的 —— 代码里实际判据是「有没有图片」**：

| 目录内容 | 结果 |
|---|---|
| 有子目录 | 保持目录 |
| 无子目录 + 有图 | 收敛成封面 |
| 无子目录 + 无图 | 保持目录 |

`ABC-01.jpg + ABC-01-A.mp4 + ABC-01-B.mp4`（无子目录）→ **收敛成一张封面，A/B 进 files 清单** ✓ 正确。

真正会被吃错的是「无子目录 + 多部片子 + 只有一张图」（`AAA-01.jpg + AAA-01.mp4 + BBB-02.mp4`）。
**当前盘上没有这类样本 → 本轮不动**（没样本就加规则 = 脑补约束）。
且这类问题**用数量判定修不好**（"视频数 ≤ 1" 会让 `ABC-01-A/B` 反而不收敛），
要做对只能看番号归一化（`ABC-01-A`/`-B` 丢后缀后同名 = 同一部），没样本不值得引入。

### 环境坑

bash 环境注入了 `ELECTRON_RUN_AS_NODE=1` → electron 退化成纯 node，
`require('electron')` 返回路径字符串、`app` undefined。
跑 electron 探针要：`env -u ELECTRON_RUN_AS_NODE ./node_modules/electron/dist/electron.exe x.cjs`

### 方法论

**"机制在但不生效"的排查顺序：先找那个写死的开关常量，别急着怀疑链路。**
这次链路（登记 → 缓存 → 回读登记 → 渲染分支）我一路查完都是通的，
问题全在一个名字字面量上。查链路能证明"没坏"，但找不到"没生效"。

---

## 第十六轮：补查交互维度（用户问「还有哪些问题（性能，损害磁盘，体验，交互）」）

**关键认识：前几轮审计口径是 BUG / 性能 / 读写 / 伤盘，`体验` 和 `交互` 两个维度是空的。**
用户这次点名了这两个，说明他自己感觉到那块没被覆盖。

### 新查到的三条（全部代码级证据，grep 计数为 0 处）

| # | 问题 | 证据 |
|---|---|---|
| 6-1 | **导航无竞态保护，谁后到谁覆盖** | `grep -rn "AbortController\|requestId\|abort()" src/` → **0 处**。`fetchFolder` 只做 `dataSource.value = data`，无序号无取消。历史面板/面包屑/返回三处**都没有 loading 守卫**（全项目只有 `handleOpen` 和 `onRefresh` 有）。可复现：连点 A（37s）再点 B（快）→ B 先到、A 后到覆盖 → **面包屑 B、内容是 A** |
| 6-2 | **导航失败静默 + 保留旧内容** | `fetchFolder` 的 `.catch` 只 `console.error` + `loadingBar.error()`，**无 notify**（全文件 `notify` 只在 `openFile` 失败处出现 1 次）；不检查 `res.ok`；`dataSource` 只在成功时赋值 → 失败时留着**上一个目录**的列表，用户以为就是这个目录 |
| 6-3 | **空目录 / 搜不到 / 加载失败，界面完全一样** | `grep -rn "n-empty\|empty-text\|无数据\|没有找到"` → **0 处**。模板只有 `v-for`，唯一线索是 `n-badge v-if="fileList.length"` 消失 |

### 厘清一个容易修错的点

6-1 和审计 1-2(a)（多扫描并发啃盘）是**同一根因的两个面**。但修法不能混：

- 前端加 `seq` 丢弃过期响应 → 解决"错乱"
- 服务端同盘串行 → 解决"伤盘"。**但不能简单"复用进行中的 Promise"** ——
  用户点 B 会拿到 A 的数据，错乱反而被固化。要么**排队**（B 白等 37 秒），
  要么**抢占**（新请求来时 kill 掉旧的 ffmpeg）。排队体验差，抢占才是对的。

### 表达方式

用户要的是**清单**，不是复盘。做法：一张分层图（会卡死 / 持续伤盘 / 交互错乱）+ 正文几行 + 细节落
`docs/AUDIT-2026-09-24.md` 第六节，并在文档开头加**状态看板**（哪条已修、哪条未修）。
另外单列"看着像问题但可能是你有意的"（Esc 返回、搜索框内 F5 失效）—— **不擅自当 BUG 报**。

---

## 第十七轮：把清单分给 4 个子代理做交叉验证 → 4 条被推翻

用户要求：「将要改动或者修复调整的清单……分给几个子代理去交叉验证还是检查，确认没问题后汇总回来，
**只处理那些成立的，不成立的确认是真的就都不处理！**」

### 分派方式（关键：提示词设计）

拆成 13 条命题 → 4 个独立子代理：
- A 服务端/磁盘（A1–A5）、B 前端/交互（B1–B5）、C 安全/内存（C1–C3）、**D 反方**（专找"被误判成缺陷的特性/影子判据/夸大影响"）

提示词里**明确写"这些是未经证实的假设，不是事实"**，并要求：
① 可复算证据（文件:行 + 原文，或自己跑的命令与原始输出）
② 标注证据等级（静态/实测/推理）
③ **主动找反例**，末尾必须附「我推翻了什么」；推不动 = 审查没到位，要重看
④ 修法涉及架构决策时**不许替用户选**

### 结果：13 条里 4 条推翻、3 条降级、6 条成立（4 条已修）

**🔴 被推翻的（教训最贵）**

1. **A2「缓存库 64% 永久垃圾」—— 撤销，核心结论是错的。**
   `nedb` 的 `loadDatabase()` **第 3 步就是压缩**（`persistence.js:250-262` 注释：
   "3) Compact the database … all data is persisted right away, which has the effect of
   compacting the database file"；`:299` `persistCachedDatabase(cb)`）。
   **我自己复核**：副本 4,261,641 → 2,887,915 字节（-32.23%），墓碑 3 → 0。
   → "垃圾永远留在文件里"是错的。自定义 `compact()` 是死代码 ✓ 但**不产生声称的后果**。
   - 教训：**我用「读了项目自己的 nedb.ts」替代了「读第三方库的真实行为」**。
     那个死掉的 `compact()` 是吸引我注意力的东西，真正干活的是依赖内部的 `loadDatabase`。

2. **A3「抽帧慢 3 倍」—— 撤销。** 实测轮换顺序 0.89~1.05x。且 fluent-ffmpeg
   **本来就用 input seek**（`recipes.js:388`），我把 current 已有的特性算成了提议方案的优势。
   - 教训：**对照组里不能混入"被测对象已经具备的东西"**，否则差异全是自己造出来的。

3. **C1「/raw 可读任意文件」—— 撤销。** `ARCH-PLAN.md` §明确不做 里有**独立一行**
   "收紧 /raw 的路径校验……再收紧只是麻烦"。我在报告里写"不在不做范围内"是**记错了** ——
   这是重开一个已被裁决的决定。

4. **B5「正则混进竖线」—— 撤销。** `|` 是 **Windows 文件名保留字符**，`item.name` 永不含它
   → 两种正则对被测对象逐字等价。实测 12 搜索词 × 8 文件名，差异数 = 0。
   - 教训：**只观察代码形状、不观察能否产生可观察差异** = 影子判据最典型的形态。

**🟡 降级的**：A5（885 个文件 → 实测 dist 只有 3 个）、B4（NaN → naive-ui 有显式回退）、
B3（"加载失败也空白"与 B2 自相矛盾）、B2（"只在成功时赋值"机理不准）、
C3（1MB/27KB → 实测 0.63MB/47KB）、A4（"0 处引用"不准，有 1 处副作用 import 且被打包）

### 已修 4 处（全部修在咽喉点，不是补丁）

| # | 位置 | 改法 | 为什么是咽喉点 |
|---|---|---|---|
| A1 | `electron/utils/thumbnail.ts` | `extractFrame` 加 20s 超时 → `kill('SIGKILL')` → `finish(false)`，`settled` 守卫 | 全项目**唯一** await 外部子进程处；`videoThumb`/`makeDirCover`/`handleCover` 全部自动受保护 |
| B1 | `src/views/FileFinder/index.vue` | `fetchFolder` 加 `fetchSeq`，过期响应不写 `dataSource` **也不收尾 loading** | 唯一写入点；5 个入口 + 未来新入口自动安全 |
| B2 | 同上 | `fetch` → `getAction` + 失败 `notify` | 响应检查收口到 `request.ts` |
| B4 | `src/components/HistoryTable/index.vue` | 两处裸 `fetch` → `getAction` | 已验证 `src/` 再无裸 fetch |

验证：B1 修复跑了 **4 场景实测**（慢先→快后 / 快先→慢后 / 单请求 / 三并发）全通过；
`vue-tsc` + `tsc` 均 exit 0。修复中撞到真错误：`cmd.kill()` 类型要求传参（TS2554）。

### 子代理新发现（我原报告完全没提到）

1. **`Origin: null` 在白名单里** → `file://`/`data:`/sandbox 页面**能完整读走**任意目录树（比 C2 严重）
2. **不校验 `Host` 头** → DNS rebinding 前提成立（只绑 127.0.0.1 不阻断）
3. **`/getFileTree` 零校验 + 递归整盘** → 任意网页一个 `<img>` 就能让主进程递归 stat 整个 C 盘
4. **`openFolderController` 无 try/catch + `event.emit` 无兜底** → handler 抛错则响应永不返回
5. `readFolder` 的 try/catch **救不了永不 settle 的 await**（A1 的机制补充）

### 待拍板（没做）

C3 内存上限（**修法有取舍**：稳定 key 会让改图不刷新；LRU 会打坏 `:lazy` 懒加载 ——
**我原报告说"淘汰是安全的"是错的**）／C2 + 三条新发现（涉及"是否给本地服务加来源校验"，
而 HTTP 形态是他拍板的）／B3 空状态（缺失的功能 ≠ 错误的行为，是产品决定）／
A4+A5（"清理"和"修错"不是一回事）

---

## 第十八轮：三个决策的明确呈现 + 审计文档内容丢失

### 用户诉求

「C3 修法取舍、B3 要不要空状态、C2 要不要给本地服务加来源校验 ——
**能不能给我明确的内容，我阅读障碍看不到也找不到**」

→ 他要的不是"去翻文档"，是**直接摆到眼前**。做法：一张图（每个决策一行：现状 + 建议）+ 正文一句话版，
完整取舍写进新文档。**并且给出我的建议**（他反复说过别把选项甩给他）。

### 我的建议

| 项 | 建议 | 一句话理由 |
|---|---|---|
| C3 缩略图内存 | **现在不动** | 库里不到 100 张 ≈ 5MB，离危险很远；三条修法各有真代价 |
| B3 空白界面 | **加一行浅灰小字** | 最需要它的是"搜索没结果"——敲了字全空会以为程序坏了 |
| C2 本机服务 | **加一次性口令 token** | 时机正好：请求刚收口到 `request.ts`，只需改两处 |
| A4 / A5 | **先不做** | 「清理」和「修错」不是一回事，单独列出来让他定 |

顺手改掉的：`thumbStore.ts` 顶部那句错注释（"内存开销为零"——"引用不是副本"对但结论错：
**正因为它强引用住了，字符串才不能被回收**）。

### 🔴 发现 `docs/AUDIT-2026-09-24.md` 有内容丢失

**现象**：我在 13:20 之后往它里面加的内容（「三视角交叉验证」整段、"5 个调用点"、
状态看板 13:35 版、第七节整节）**全部没落盘** —— 文件停在了 13:20 的版本（缺第五、七节）。
而**每一次 Edit 都返回了"成功"**。`git status` 显示它是正常的已跟踪文件。

**处置**：不再对它做增量编辑（怕越改越坏），把交叉验证内容**独立写成**
`docs/AUDIT-VERIFY-2026-09-24.md`（一次 Write 写完），并在开头声明「冲突时以本文为准」。

**教训（已写进 skill 坑表）**：
① 对同一文件做多次超长增量编辑后，**必须 grep 抽查关键短语**确认真的落盘，别只看"成功"提示；
② 一旦发现丢失，**改用整体 Write 重写**，不要继续增量补；
③ 报告类内容**一开始就按主题拆成多个文件**，每个文件写一次，比往一个文件反复追加可靠。

### 本轮所有写入的落盘抽查（8 项全过）

`超长编辑坑 / 收尾检查清单标题 / 子代理交叉验证节 / AUDIT-VERIFY 待拍板 /
thumbStore 错注释 / FRAME_TIMEOUT_MS / fetchSeq / getAction` —— 全部 grep 命中 = 1 或以上 ✓

---

## 第十九轮（傍晚）：图片空白卡片 + C3/B3/C2 三件收尾

用户消息：「C3如果有必要就修了，B3和C2你确保没问题不会有性能问题和体验问题就修复了，
**主要是C3有哪些修复方案**？ 还有这种是什么情况？图片没加载出来 @截图 D:/sample/videos/TST-PPV-1000001」

### A. 图片空白卡片 —— 根因是「承诺 > 能力」，不是「WebP 不支持」

**7 步证据链（全部实测）**：
1. `ls` → 是目录（`TST-PPV-1000001.jpg` 8042B + 同名 mp4 3.86 GB）
2. 缓存记录里它 `type=image`、`files=1`、`size` = 片子总和 → **收敛判定是对的**
3. 同记录另外 40 个条目都有 `thumbData`，**只有它没有** → 不是普遍问题
4. `od` → 头 12 字节 `52494646 621f0000 5745425056503820` = `RIFF....WEBP`
   → **是改了 `.jpg` 后缀的 WebP**（750×421）
5. electron 探针：`nativeImage.createFromPath` → `isEmpty=true 0×0`；
   同字节改名 `.webp` 仍 `true`；真 JPEG 改名 `.webp` → `800×539`
   → **扩展名无关，是内容格式**
6. 扫 D:/sample/videos（45 目录/31 图）+ E:/sample/videos（34 目录/13 图）
   → **44 张封面里只有这 1 个 WebP**
7. 内置 ffmpeg 能解 → 480×269 真 JPEG

**真根因**：`server/index.ts` 选封面正则 `/\.(jpe?g|png|bmp|gif|svg|psd|webp)$/i`
**放行 webp/psd/svg**，而 `nativeImage` 这三样一个都解不了 —— 矛盾跨两个文件，单看哪个都不算错。

**改法（咽喉点）**：
- `thumbnail.ts`：抽 `runFfmpeg(cmd, what, src)` 作为「超时 + SIGKILL + settled」的**唯一**一处；
  `imageThumb` 改 `async`：nativeImage 解不开 → **先 `access` 确认存在** → ffmpeg 转 PNG
  （`scale='min(960,iw)':-2`）→ 回到**同一条** `scaleToThumb`；`FRAME_TIMEOUT_MS`→`FFMPEG_TIMEOUT_MS`
- `videoThumb`：`return imageThumb(frame)` → `return await imageThumb(frame)`
  （不 await 会让 `finally` 先删临时帧 → 解码读到不存在的文件）
- `server/index.ts`：`makeDirCover` 从"盲试解码"改**先 `access` 再解码**
  （兜底必须先分清"不存在"和"解不开"；盲试不挡会给每个目录白起 1~2 个子进程）
  —— 常态成本不变：`access` 1 次 stat == 原来 1 次失败的 open

**E2E 实测**（真实服务端，端口改 30613 避开用户运行中的应用，DB 指向临时目录）：
```
FC2 thumb:  (无) → B6C5-muf4hgi8-54yqrn
/thumb:     HTTP 200 / 12155B / image/jpeg / 头 ffd8ffe0
无缩略图条目: 15（14 文件夹 + FC2）→ 14（全是文件夹，零回归）
中转文件残留: 0
```

### B. C3 → **决定不动**（实测数字推翻了"该修"的直觉）

直接解析真实 `searchCache.db`：
- 缓存条目 **197**；带缩略图字段 **160**；单张平均 **45,386 字符**（最长 87,811）
- 全部缩略图 base64 合计 **6.93 MB**；DB 6.96 MB
- ⚠️ **修正自己的口径**：V8 对纯 ASCII 串用**单字节**表示，所以不是 ×2 的 13.85 MB
- 重启即归零；整库全刷一遍 ≈ 7 MB，单个常用目录 ≈ 1.8 MB

三条修法的真代价（**唯一"结构上正确"的那条动面最大**）：
| 方案 | 代价 |
|---|---|
| 稳定 key（路径+mtime+size 派生） | 动 3 个调用点；`makeDirCover` 得先 stat；**改浏览器缓存语义**（同名换图后不刷新） |
| LRU 上限 | `<n-image :lazy>` 未取过图的格子滚动到时**白图** |
| 按 key 回查 nedb | 要给嵌在数组里的 key 建索引 → nedb 从"一目录一文档"变"一缩略图一文档"，**写盘次数显著上升**，与项目目标相冲 |

**触发条件（写下来，将来不用重新论证）**：单会话累计 > 200 MB（≈ 整库全刷 30 次）再动。

### C. C2 口令 —— 已做；**推翻了我自己原报告的估计**

原报告说"只需改两处（加个 header + 服务端校验一次）"—— **错了**。
`<img src>` **带不了自定义请求头**，而缩略图/预览图正是 `<img>` 取的
→ 口令只能走 URL（`?t=`），渲染层必须多一个 `apiUrl()` **唯一出口**。
"加 header" 那条整个作废（也就不再需要 CORS 预检）。

落地：`electron/server/token.ts`（新增，`randomBytes(24)`，不落盘）
+ `server/index.ts` 在 `http.createServer` 回调（**所有请求的唯一入口**）校验
+ `main/index.ts` `ipcMain.on('ff-token', e => e.returnValue = LOCAL_TOKEN)`
+ `request.ts` `ipcRenderer.sendSync('ff-token')` + `apiUrl()`；`thumbUrl`/`rawUrl` 过 `apiUrl`

实测：无口令/错口令/空串 → 403；对口令 → 200；
无口令 `/getFileTree` → **403（2ms 内拒绝，不再递归整盘）**；`/thumb` 无口令 → 403；
IPC 频道名两边一致 ✓ 且渲染层确实拿到口令 ✓（用与 main 相同 webPreferences 的真窗口验的）

### D. B3 空状态 —— 已做，但实施中发现原报告漏了一条约束

三种空白（目录真空 / 搜索无结果 / 请求失败）要分开。**失败时不能显示"这个目录是空的"** —— 那是骗人。
→ 新增 `loadFailed` ref（失败置真、每次取数开头清掉），失败时压掉提示。
优先级：loading/失败 → 不显示；有搜索词零结果 →「没找到匹配的内容」；没选目录 → 不显示；其余 →「这个目录是空的」

### E. 本轮踩的坑（已进 skill）

1. **探针对照组掺假，复发了一次**：第一版探针的"真 JPEG 对照组"是用**空图**的 `toJPEG()`
   生成的 → 出来还是空图 → 整个结论是噪声。重做（拿盘上真 JPEG）才拿到干净证据。
2. **`USERPROFILE` 一改，`app.whenReady()` 永不触发**（Electron 20 拿它定位 profile）
   → 隔离 DB 只能在 `whenReady` 之后、`require` 服务端**之前**改环境变量
3. **Electron 20 跑 Node 16，没有全局 `fetch`** → 探针里用 `node:http`
4. **沙箱无 GPU** → `app.disableHardwareAcceleration()` + `disable-gpu`，否则一直重试到 FATAL
5. **esbuild 0.14 的 `buildSync` 不支持 plugin**（"Cannot use plugins in synchronous API calls"）→ 用异步 `build()`
   - 顺带：用 onLoad plugin **替换端口 + 追加 `export`**，就能在不动源码的前提下起一个隔离的真实服务端
6. **Electron 20 不支持 `-e` 执行内联脚本**（有脚本文件才行）

### F. 待用户动作
- **刷新窗口**（主进程已被 vite-plugin-electron 自动重建，`dist/electron/main/index.js` 14:01 起已含 `ff-token`）
- 对 `D:/sample/videos/TST-PPV-1000001` 按一次 **↻**（旧缓存记录还是空的，`findCache` 命中就不会重扫）
- A4/A5 仍不做；`list.txt` 未重建

---

## 第二十轮：从缓存记录打开没有面包屑/返回（用户自己找到线索）

用户报：「从选择文件夹进入的可以正常触发，但从缓存记录打开的，比如打开 videos 再打开 videos/xxx
就不会有类似文件夹的多层级的那种面包屑和返回」+「我那个路径返回你给我搞不见了？」

### 关键结论：**是原有缺陷，不是本轮改出来的**

`git show HEAD:src/views/FileFinder/index.vue`（用户自己的提交 `65516c5 添加备份功能`）
里 `openFolderInCover` 就已经「push 一层但不设 `dir`、不传 name」，
`openHistory` 就已经按 `mode` 分叉。**先取证再回答**，避免背锅/甩锅。

### 根因：两处独立的不一致

**① 返回按钮 `v-if="dir"` 用错了变量**
`dir` = 「文件夹选择框里选了什么」，与导航栈无关；`onBack` 自己判的是 `openStack.length === 1`。
`openHistory` 的 `mode==='cover'` 分支走 `openFolderInCover` → **从不设 `dir`** → 返回按钮不出现。

**② 层级名 `name` 是可选参数，靠调用方记得传**
`IOpenInfo.name?: string`。三条件入口只有「从网格下钻」传了；模板 `v-if="!!folder.name"`
把另外两条入口的层级**整个吃掉** → "没有面包屑"。

**③ 更根本**：`openHistory` 按**缓存记录的 mode** 分叉 —— mode 是记录属性，不该决定导航形状。

### 改法（全部收口）

- `openHistory(path)` → 直接 `handleDirChange(path)`，不再看 mode
- 新增 `levelName(path)` = 路径末段（唯一实现）
- 新增 `pushLevel(path, mode)` = **唯一**建栈出口（全库 `openStack.value.push` 只剩这 1 处）
- `IOpenInfo.name` 改成**必填** → 「某一层没名字」在类型上不可能
- `openFolderInCover` 去掉 `name` 参数（两个调用点同步改）

### 验证

- `levelName` 用**真实路径**跑 25 例（含中文「結城モナ」「示例演员J」、尾斜杠、`E:/` 盘根、
  重复分隔符 `C:////Users////x`）→ 判错 0（方法是**从源文件里抠出真实实现** eval，不是重打一遍）
- `vue-tsc --noEmit` exit 0；`tsc -p tsconfig.electron.json --noEmit` exit 0
- 建栈收口：`openStack.value.push` = 1 处；两参调用 = 0 处

### 两个附加发现（都告诉用户了）

1. **有一条观感会变**：「选择文件夹」那条路第一级现在也会显示 tag（以前不显示）——
   这是同一个 name 缺陷的另一半，修了才有"多层级面包屑"。给了回退选项。
2. **用户的 dev server 已经不在跑了**：`3344` / `5173` 都没有 vite；
   `[::1]:5173` 上那个 PID 24076 是**代理**（返回 `502 upstream connect failed: os error 10061`）。
   → 本体进程还活着（electron PID 3556 占着 3060），渲染层是内存里的旧页面，
   **本次修复要重启 `npm run dev` 才会生效**。
   - 教训：`netstat | grep '^127\.0\.0\.1'` **会漏掉 IPv6 的 `[::1]` 监听** —— 先按端口全量列。

---

## 依赖全量升级（傍晚，用户点名要求）

**结论**：升到 electron 44 / vite 7 / vue 3.5 / naive-ui 2.45 / TS 5.9 / vue-tsc 3 /
electron-builder 26 / ffprobe 2，**vite 8 被否**（见 MEMORY.md 第六节硬契约 2）。
删掉 3 个零引用依赖：`shelljs`（全库 0 引用，连 HEAD 里都没有）、`@vicons/material`（同）、
`iconv-lite`（只在 vite.config 的 renderer 名单里出现过一次，源码零 import —— 那个名单本身也是坏的）。

**升级过程中抓到的 3 个真 BUG（都是"编译通过但运行期炸"）**

1. **vpe 1.x 默认内联 `dependencies`** → 主进程 bundle 里出现
   `__dirname.substr(0, __dirname.indexOf('node_modules'))` → 打包后 ffmpeg 定位必失败。
   修法：官方开关 `notBundle()`（main + preload 各挂一个）。自检产物从 137 kB 回到 15.6 kB。
2. **vite 8 把渲染层的 `require("electron")` 优化成裸 ESM import** → `file://` 下渲染层白屏。
   定位靠隐藏窗口探针（`#app` 子节点数 0 + `Failed to resolve module specifier "electron"`）。
   修法：降到 vite 7.3.6 + renderer 0.14.7。
3. **npm 在 Windows 上丢了 3 个文件**（`agent-base`/`http-proxy-agent`/`https-proxy-agent`
   的 `dist/index.js` 被改名成 `*.DELETE.<hash>` 却没删掉）→ `electron-builder` 报
   `Cannot find module .../dist/index.js`。修法：删这 3 个包目录重装。

**验证（都留了原始输出）**
- `npm run typecheck` exit 0（vue-tsc 3 + tsc electron）
- `vite build` exit 0；主进程 require 列表与升级前逐项一致；渲染层 `from"electron"` 计数 = 0
- 隔离探针（无头 Electron 44，端口 3061 / 数据目录 `_probe_ver/data`）：
  无口令 403 / 带口令 200 / 404 / 错口令 403；`nativeImage` 解 PNG ok、不存在路径 → `isEmpty=true`
- 隐藏窗口加载**打包产物** `dist/index.html`：`#app` 子节点 1、正文 `请选择文件夹(D) 搜索 S`、
  `sendSync('ff-token')` 正常
- 隐藏窗口加载 **dev server** `http://127.0.0.1:3344/`：`[vite] connected` + 同样渲染成功
- `electron-builder --dir` exit 0；包内 `fluent-ffmpeg`/`nedb`/`dayjs` 都在、`shelljs` 0 条；
  `app.asar.unpacked` 里 ffmpeg.exe/ffprobe.exe 仍在
- 用户真实缓存全程未被触碰（探针数据落在 `_probe_ver/data`，真实 `searchCache.db` 时间戳早于探针）

**未实测 / 遗留**
- 界面**渲染得对不对、交互对不对**，无头环境验不了 —— 要用户自己跑 `npm run dev` 目视
- `release-verify/`（我打包验证的产物，~280 MB）**沙箱回收站失败，删不掉**，已加进 .gitignore，请用户在资源管理器里删
- Node 24 弃用警告：我们 server 里的 `url.parse`、`nedb` 里的 `util.isArray`（现在不炸，未来会）
- `less` 挂在 `dependencies`（构建期工具，让包里多 136 个文件），挪位置属结构变更，未动
- 回滚备份在 `.deps-backup/`（已 gitignore）：`package.json.bak` + `package-lock.json.bak`

---

## 启动崩溃修复：`nedb` → `@seald-io/nedb`（15:10–15:30）

**用户报**：一运行就报错 —— `Uncaught TypeError: util.isDate is not a function`（截图）。

**根因（实测）**：Electron 44 的 Node 是 **v24.21.0**，`util.is*` 只剩 `isArray`/`isDeepStrictEqual`；
`nedb@1.8.0`（2016 停更、`npm view nedb version` 就是 1.8.0）内部用了 `util.isDate`/`util.isRegExp`。
触发链：`electron/server/index.ts:810` 的 `dropLegacyRecords()` → `count({v:{$ne:2}})`。
最小复现留了原始输出（`isPrimitiveType (nedb/lib/model.js:149)`）。
**这是上一轮的漏测**：探针只请求 `/` 健康检查，没走到 nedb 查询路径。

**决策**：换维护中的续作 `@seald-io/nedb@4.1.2`，**否掉了给 `util` 打补丁的方案**
（`util.isArray` 已是 DEP0044，下轮 Node 升级照样复发 = 打补丁）。

**改动（2 个文件）**
- `package.json`：`nedb@^1.8.0` → `@seald-io/nedb@^4.1.2`；删 `@types/nedb`
- `electron/server/nedb.ts`：import / `loadDatabase`+`ensureIndex` 补回调 / `loadMeta` 投影改走 Cursor /
  `compact()` 改用公开 `compactDatafile(cb)`；其余 9 处调用一字未动

**验证（实测）**
- `npm run typecheck` exit 0；`vite build` exit 0，主进程 **15.45 kB**
- 产物自检：主进程有 `require("@seald-io/nedb")`、无 `nedb`；渲染层 `from"electron"` = 0
- 模块级 e2e（真实 `nedb.ts` + **真实库副本**，esbuild `--packages=external`）：**12/12**
- 应用级：无头启动**构建产物主进程**（隔离数据目录 + 真实库副本）→ `GET /` 200、
  `GET /getHistory` **200 total=22**（真实记录、无 `data`）、`GET /getDisks` 200、**无 `util.isDate`**
- 数据兼容：真实 8.2 MB 库可读（22 条、`findOne` 命中 `data` 13 条带 `thumbData`），
  写出格式同构；`$ne` 匹配"字段缺失"的语义用旧库复验 **3/3** —— `dropLegacyRecords` 仍正确
- 用户真实 `searchCache.db` **全程未被触碰**（探针用副本）

**未实测 / 遗留**
- `npm run dev` 的**窗口目视**没跑（会弹窗）—— 主进程层与构建产物逐字相同，但需用户看一眼
- **打包未验成**：`electron-builder --dir` 被沙箱删除护栏挡住（它要清上一轮遗留的 `release-verify/`，
  `SAFE_DELETE_BULK_CONFIRM_REQUIRED count:79`）→ 需用户自己删掉 `release-verify/` 再跑 `npm run build`
- 发现 `node_modules` 有 **178 个 npm 残留空壳目录**，命名是 `.<包名>-<hash>`（不是上轮记的 `*.DELETE.*`），
  全是空的、不影响运行
- 本轮建的 `_probe_nedb/`（探针 + 真实库副本）需要清理；`release-verify/` 仍在
